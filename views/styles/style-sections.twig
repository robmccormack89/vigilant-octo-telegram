<!-- header -->
<style>
  /* main menu active item after effects */
  /* #SiteHeader .bottom_nav_center .menu-item.current-menu-item:after {
    width: 33px;
    height: 65px;
    content: '';
    top: 11%;
    left: 36%;
    z-index: 1;
    margin: 0;
    display: block;
    position: absolute;
    background: url(/wp-content/themes/sixstar-theme/assets/img/menuactive.png) no-repeat center center;
  } */
</style>
  
<!-- woo -->
<style>
  .product-grid-item .uk-card {
      border-radius: 5px;
  }
  .product-grid-item.product .uk-card-body {
      padding: 20px;
  }
  #ProductTitle .uk-card-title {
      margin-bottom: 10px;
      color: {{dark_bg}};
      font-size: 1.3rem;
      font-weight: 800;
      letter-spacing: 0px;
  }
  #ProductMedia img {
      border-radius: 5px 5px 0px 0px;
  }
  
  .woocommerce-mini-cart__empty-message {
    padding: 20px;
  }
  .woocommerce-MyAccount-navigation a {
      font-weight: 600;
      color: #151c28;
  }
  .woocommerce #content div.product div.images, .woocommerce-page #content div.product div.images, .woocommerce-page div.product div.images, .woocommerce div.product div.images {
      width: 100%;
  }
</style>

<!-- post -->  
<style>
  .the-post .uk-card-body {
    padding: 30px;
  }
  .the-post a.cat-link {
    font-size: 13px;
    font-weight: 600;
    text-transform: uppercase;
    color: {{grey_text}};
    line-height: 100%;
    display: inline-block;
  }
  .the-post .post-date {
    font-size: 13px;
    color: {{grey_text}};
    line-height: 1.6rem;
    font-weight: 600;
  }
</style>
  
<!-- infinite scroll loader animation, css only -->
<style>
  .preloader {
    align-items: center;
    pointer-events: none;
    background: rgb(255, 255, 255);
    display: flex;
    height: 100vh;
    justify-content: center;
    left: 0;
    position: fixed;
    top: 0;
    transition: opacity 0.5s linear;
    width: 100%;
    z-index: 9999;
  }
  .page-load-status {
    display: none;
    /* hidden by default */
    padding-top: 20px;
    text-align: center;
    color: #777;
  }
  .loader-ellips {
    font-size: 20px;
    /* change size here */
    position: relative;
    width: 4em;
    height: 1em;
    margin: 10px auto;
  }
  .loader-ellips__dot {
    display: block;
    width: 1em;
    height: 1em;
    border-radius: 0.5em;
    background: {{orange_primary}};
    /* change color here */
    position: absolute;
    animation-duration: 0.5s;
    animation-timing-function: ease;
    animation-iteration-count: infinite;
  }
  .loader-ellips__dot:nth-child(1),
  .loader-ellips__dot:nth-child(2) {
    left: 0;
  }
  .loader-ellips__dot:nth-child(3) {
    left: 1.5em;
  }
  .loader-ellips__dot:nth-child(4) {
    left: 3em;
  }
  @keyframes reveal {
    from {
      transform: scale(0.001);
    }

    to {
      transform: scale(1);
    }
  }
  @keyframes slide {
    to {
      transform: translateX(1.5em)
    }
  }
  .loader-ellips__dot:nth-child(1) {
    animation-name: reveal;
  }
  .loader-ellips__dot:nth-child(2),
  .loader-ellips__dot:nth-child(3) {
    animation-name: slide;
  }
  .loader-ellips__dot:nth-child(4) {
    animation-name: reveal;
    animation-direction: reverse;
  }
</style>